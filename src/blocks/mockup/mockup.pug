mixin mockup(data)
  +b.mockup(style=`background-image: url(${data.preview})`)
    +e('a').link(data-origin=`${data.origin}`)
    +e.content
      +e('h4').title
        | #{data.title}
      +e.date
        | #{data.date}
